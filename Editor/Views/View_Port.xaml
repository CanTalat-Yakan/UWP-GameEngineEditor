<Page
    x:Class="Editor.View_Port"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Editor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:my="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls" 
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid MinHeight="1" MinWidth="1">
        <Grid Height="10000"/>
        <SwapChainPanel x:Name="x_SwapChainPanel_ViewPort"/>

        <controls:TabbedCommandBarItem HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20">
            <AppBarButton Margin="0,0,-10,0">
                <AppBarButton.Flyout>
                    <Flyout Placement="BottomEdgeAlignedRight">
                        <TextBlock x:Name="x_TextBlock_Debug_FPS"/>
                    </Flyout>
                </AppBarButton.Flyout>
                <AppBarButton.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xF167;"/>
                </AppBarButton.Icon>
            </AppBarButton>

        </controls:TabbedCommandBarItem>
    </Grid>

</Page>
